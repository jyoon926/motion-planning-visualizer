import type { Point, Rectangle } from '../utils/types'

export const computeVoronoi = (rectangles: Rectangle[], start: Point, end: Point) => {
  // Placeholder implementation
  return {
    nodes: [start, end],
    edges: [[start, end]],
  }
}
